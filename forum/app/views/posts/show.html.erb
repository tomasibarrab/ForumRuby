	<h1>
	    <%= @post.title %><br>
  	</h1>
	<p>
		<%= @post.content %><br>
	</p>

	<%= link_to 'Edit', edit_post_path(@post) %>
	<%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm: 'Are you sure?'} %><br><br><br>
	<%= link_to 'Home', root_path %>
